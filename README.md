# base9

A new Flutter project.

## Icons

App icon source: `assets/icon/base9.svg`. After changing the SVG, regenerate launcher icons (Android mipmaps + Windows .ico):

```bash
dart run tool/generate_icons.dart
```
